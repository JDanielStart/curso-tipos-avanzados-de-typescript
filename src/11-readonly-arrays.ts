const numbers: ReadonlyArray<number> = [1, 2, 2, 2];
//Esto ya no deja hacerlo
/* numbers.push(1);
numbers.pop();
numbers.unshift(1); */

numbers.filter(() => {});
numbers.reduce(() => 0);
numbers.map(() => 0);
